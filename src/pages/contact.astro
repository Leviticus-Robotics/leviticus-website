---
import Card from "../components/ui/Card.astro";
import DisplayHeading from "../components/ui/DisplayHeading.astro";
import Eyebrow from "../components/ui/Eyebrow.astro";
import Lead from "../components/ui/Lead.astro";
import PageShell from "../components/ui/PageShell.astro";
import SectionHeader from "../components/ui/SectionHeader.astro";
import { contactLinks } from "../data/site";
import BaseLayout from "../layouts/BaseLayout.astro";
---

<BaseLayout
	title="Contact"
	description="Contact FTC team 36099 Leviticus by email or Instagram."
>
	<section aria-labelledby="contact-title">
		<PageShell>
			<SectionHeader>
				<Eyebrow>Contact</Eyebrow>
				<DisplayHeading id="contact-title">Start a conversation.</DisplayHeading>
				<Lead>
					Sponsors and other interested parties can reach us using the methods below.
				</Lead>
			</SectionHeader>
			<div class="grid grid-cols-2 gap-4 max-[840px]:grid-cols-1">
				{
					contactLinks.map((contact) => (
						<Card as="article" class="w-full max-w-lg">
							<Eyebrow>{contact.label}</Eyebrow>
							<h2 class="mb-0 text-[clamp(1.25rem,2.4vw,1.7rem)] leading-[1.15]">
								<a
									class="text-brand-purple-strong [overflow-wrap:anywhere] decoration-brand-purple/35 underline-offset-[0.2em]"
									href={contact.href}>{contact.value}</a
								>
							</h2>
						</Card>
					))
				}
			</div>
		</PageShell>
	</section>
</BaseLayout>
